server:
  port: 9006
spring:
  application:
    name: config.server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rakeshsonkar/micro.config.project
          clone-on-start: true