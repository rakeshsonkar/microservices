server:
 port: 9000
spring:
  datasource:
   url: jdbc:mysql://localhost:3306/userservices
   username: root
   password:
   driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
     hibernate:
      ddl-auto: update
     show-sql: true
